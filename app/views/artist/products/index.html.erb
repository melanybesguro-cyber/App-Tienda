<h1>Mis productos</h1>

<% if params[:category_id].present? %>
  <p>Productos de la categoría seleccionada.</p>
  <%= link_to "Ver todos los productos", artist_products_path %>
<% end %>

<p>
  <%= link_to "Mis productos", artist_products_path %>
</p>

<hr>

<%= link_to "Nuevo producto", new_artist_product_path %>

|

<%= link_to "Administrar categorías", artist_categories_path %>

<hr>

<% if @products.any? %>

  <% @products.each do |product| %>

    <div>
      <h2><%= product.name %></h2>

      <% if product.image.attached? %>
        <%= image_tag product.image, alt: product.name, width: 240 %>
      <% end %>

      <p>
        <strong>Descripción:</strong>
        <%= product.description %>
      </p>

      <p>
        <strong>Precio:</strong>
        $<%= product.price %>
      </p>

      <p>
        <strong>Stock:</strong>
        <%= product.stock %>
      </p>

      <p>
        <strong>Categoría:</strong>
        <%= product.category.name %>
      </p>

      <%= link_to "Ver",
          artist_product_path(product) %>

      |

      <%= link_to "Editar",
          edit_artist_product_path(product) %>

      |

      <%= link_to "Eliminar",
          artist_product_path(product),
          data: {
            turbo_method: :delete,
            turbo_confirm: "¿Seguro que querés eliminar este producto?"
          } %>
    </div>

    <hr>

  <% end %>

<% else %>

  <p>Todavía no tenés productos publicados.</p>

<% end %>